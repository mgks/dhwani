{
  "name": "dhwani-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=dhwani-web",
    "build": "npm run build --workspace=dhwani-web",
    "publish-lib": "npm publish --workspace=dhwani --access public"
  }
}